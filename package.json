{
  "name": "inbot-ticket-management",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22 <23",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present run build",
    "typecheck": "pnpm --recursive --if-present run typecheck",
    "test": "pnpm --recursive --if-present run test",
    "format": "prettier --write package.json pnpm-workspace.yaml tsconfig.base.json backend frontend shared",
    "format:check": "prettier --check package.json pnpm-workspace.yaml tsconfig.base.json backend frontend shared"
  },
  "devDependencies": {
    "prettier": "3.5.3"
  }
}
